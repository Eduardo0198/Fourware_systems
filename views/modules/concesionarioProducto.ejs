<% function getImgUrl(url) {
    if (!url || url === 'producto.png') {
        return '/img/ppg-logo.png';
    }
    return url;
} %>

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Detalle del Producto</h2>
    <a href="/concesionario/catalogo" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Volver al catálogo
    </a>
</div>

<% if (mensaje) { %>
    <div class="alert alert-<%= mensaje.tipo %> mb-3" role="alert">
        <%= mensaje.texto %>
    </div>
<% } %>
<hr>

<div class="producto-detalle">
    <div class="detalle-imagenes">
        <% if (producto.imagenes && producto.imagenes.length > 1) { %>
            <div id="productoCarousel" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner">
                    <% producto.imagenes.forEach((img, index) => { %>
                        <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                            <img src="<%= getImgUrl(img) %>" class="d-block w-100" alt="<%= producto.nombre %>">
                        </div>
                    <% }); %>
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#productoCarousel" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Anterior</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#productoCarousel" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Siguiente</span>
                </button>
            </div>
        <% } else { 
            let singleImg = (producto.imagenes && producto.imagenes[0]) || producto.imagen;
        %>
            <img src="<%= getImgUrl(singleImg) %>" alt="<%= producto.nombre %>" class="img-fluid">
        <% } %>
    </div>

    <div class="detalle-info">
        <h3><%= producto.nombre %></h3>
        <div class="product-attributes">
            <div class="attribute-row">
                <span class="attribute-label">Descripción:</span>
                <span class="attribute-value"><%= producto.descripcion || 'Sin descripción' %></span>
            </div>
            <div class="attribute-row">
                <span class="attribute-label">Precio unitario:</span>
                <span class="attribute-value">$<%= producto.precio_unitario %></span>
            </div>
            <div class="attribute-row">
                <span class="attribute-label">Unidad de venta:</span>
                <span class="attribute-value"><%= producto.unidad_venta || 'No especificada' %></span>
            </div>
            <div class="attribute-row">
                <span class="attribute-label">Peso unitario:</span>
                <span class="attribute-value"><%= producto.peso_unitario %> kg</span>
            </div>
            <div class="attribute-row">
                <span class="attribute-label">Volumen unitario:</span>
                <span class="attribute-value"><%= producto.volumen_unitario %> m³</span>
            </div>
            <div class="attribute-row">
                <span class="attribute-label">Medida primaria:</span>
                <span class="attribute-value"><%= producto.medida_primaria %></span>
            </div>
            <div class="attribute-row">
                <span class="attribute-label">SKU:</span>
                <span class="attribute-value"><%= producto.SKU %></span>
            </div>
        </div>

        <form method="POST" action="/concesionario/carrito/agregar" class="mt-4">
            <input type="hidden" name="sku" value="<%= producto.SKU %>">

            <div class="mb-3">
                <label for="cantidad" class="form-label fw-semibold">Cantidad</label>
                <input type="number"
                       id="cantidad"
                       name="cantidad"
                       class="form-control"
                       min="1"
                       step="1"
                       value="1"
                       required>
            </div>

            <button class="btn btn-primary w-100">Agregar al carrito</button>
        </form>
    </div>
</div>
